function numberReducer(state, action) {
  switch (action.type) {
    case "increment":
      return { ...state };
      break;
    case "increment":
      return { ...state };
  }
}
export default numberReducer;
